<%= form_for([post, @comment]) do |f| %>
    <%= f.label :content %><br>
    <%= f.text_field :content %>

    <%= f.submit %>

    <% end %>

<!-- NHO: What's the difference between `_new` and `_form`? -->
